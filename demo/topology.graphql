query ElementTopologyDemo {
  model(name: "2026-SampleModel") {
    elements(where: { id: "0Czv28JAHDuepx33rj$lAZ" }) {
      guid
      name
      type
      intersects(where: { type: "Wall" }) {
        guid
        name
        type
      }
      adjacent(where: { type: "Column" }) {
        guid
        name
        type
      }
    }
  }
}
